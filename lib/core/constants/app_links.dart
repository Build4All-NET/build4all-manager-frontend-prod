/// Fixed external links used across the app.
///
/// These are static front-end constants (no backend endpoint required).
class AppLinks {
  AppLinks._();

  /// Support / ticketing portal where the owner can open a ticket.
  static const String ticketsUrl = 'https://tickets.build4all.net';
}
